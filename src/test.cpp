// #include "Parser/parser.h"
#include <iostream>
#include <cmath>

int main() {
  // s21::Parser parser;
  // parser.Parsing("../dataset/emnist-letters-train.csv");
  // parser.Print();
  // return 0;
  printf("%lf", 1.0 / 1.0 + exp(0.00000000001 * (255.0 - 0.5)));
}